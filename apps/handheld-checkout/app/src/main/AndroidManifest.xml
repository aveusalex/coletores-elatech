<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Necessária apenas porque é a permissão configurada no broadcast do coletor. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            v0.2.0: escuta as ações configurável e embutidas. A entrega real neste
            firmware é `com.xcheng.scanner.action.BARCODE_DECODING_BROADCAST`.
        -->
        <receiver
            android:name=".scanner.ScanResultReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.scanResult" />
                <action android:name="com.xcheng.scanner.action.BARCODE_DECODING_BROADCAST" />
                <action android:name="com.xcheng.scanner.action.BARCODE_DECODING_BROADCAST_INPUT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
