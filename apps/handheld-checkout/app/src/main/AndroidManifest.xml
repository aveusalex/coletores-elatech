<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Necessária apenas porque é a permissão configurada no broadcast do coletor. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Só a ação explícita fica no manifesto. No Android 13 as ações implícitas do
            firmware (BARCODE_DECODING_BROADCAST[_INPUT]) não chegam a receiver de
            manifesto — são tratadas pelo receiver registrado em runtime na MainActivity.
        -->
        <receiver
            android:name=".scanner.ScanResultReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.scanResult" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
